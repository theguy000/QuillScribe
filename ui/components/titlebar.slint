import { ThemeColors } from "../themes/theme-definitions.slint";

export component TitleBar {
    in property <ThemeColors> theme-colors;

    callback drag-window();
    callback minimize-window();
    callback close-window();

    height: 36px;

    Rectangle {
        background: theme-colors.titlebar_bg;

        // Bottom border matching original
        Rectangle {
            height: 1px;
            background: theme-colors.border_light;
            y: parent.height - 1px;
        }

        property <bool> drag-toggle: false;

        // Two TouchAreas alternate via drag-toggle so the active one
        // instantly drops the OS pointer grab on click.
        ta1 := TouchArea {
            enabled: !drag-toggle;
            pointer-event(event) => {
                if event.kind == PointerEventKind.down {
                    drag-toggle = true;
                    drag-window();
                }
            }
        }

        ta2 := TouchArea {
            enabled: drag-toggle;
            pointer-event(event) => {
                if event.kind == PointerEventKind.down {
                    drag-toggle = false;
                    drag-window();
                }
            }
        }

        HorizontalLayout {
            padding-left: 14px;
            padding-right: 0px;
            alignment: space-between;

            HorizontalLayout {
                alignment: start;
                spacing: 10px;

                Rectangle {
                    width: 24px;
                    height: 24px;
                    border-radius: 8px;
                    background: theme-colors.accent.with-alpha(0.10);
                    y: (parent.height - self.height) / 2;

                    Image {
                        source: @image-url("quill.svg");
                        colorize: theme-colors.accent;
                        width: 16px;
                        height: 16px;
                        x: (parent.width - self.width) / 2;
                        y: (parent.height - self.height) / 2;
                    }
                }

                Text {
                    text: "QUILLSCRIBE";
                    color: theme-colors.titlebar_text;
                    font-size: 12px;
                    font-weight: 700;
                    letter-spacing: 0.08rem;
                    vertical-alignment: center;
                }
            }

            HorizontalLayout {
                alignment: end;
                spacing: 0px;

                // Minimize button — SVG-like thin horizontal bar
                Rectangle {
                    width: 44px;
                    height: 36px;

                    property <color> minimize-foreground: theme-colors.titlebar_text;

                    Rectangle {
                        width: 10px;
                        height: 1px;
                        background: minimize-foreground;
                        x: (parent.width - self.width) / 2;
                        y: (parent.height - self.height) / 2;
                    }

                    ta-min := TouchArea {
                        clicked => { minimize-window(); }
                        mouse-cursor: pointer;
                    }

                    background: ta-min.has-hover ? theme-colors.bg_tertiary.with-alpha(0.78) : transparent;

                    animate background { duration: 150ms; }
                    animate minimize-foreground { duration: 150ms; }

                    states [
                        hovered when ta-min.has-hover: {
                            minimize-foreground: theme-colors.text_primary;
                        }
                    ]
                }

                // Close button — × character matching original SVG X
                Rectangle {
                    width: 44px;
                    height: 36px;

                    property <color> close-foreground: theme-colors.titlebar_text;

                    Text {
                        text: "×";
                        color: close-foreground;
                        font-size: 20px;
                        font-weight: 300;
                        horizontal-alignment: center;
                        vertical-alignment: center;
                    }

                    ta-close := TouchArea {
                        clicked => { close-window(); }
                        mouse-cursor: pointer;
                    }

                    background: ta-close.has-hover ? theme-colors.danger : transparent;

                    animate background { duration: 150ms; }
                    animate close-foreground { duration: 150ms; }

                    states [
                        hovered when ta-close.has-hover: {
                            close-foreground: #ffffff;
                        }
                    ]
                }
            }
        }
    }
}
